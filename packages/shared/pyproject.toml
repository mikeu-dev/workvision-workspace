[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "workvision-shared"
version = "0.1.0"
description = "Shared spatial and geometry utilities for WorkVision AI"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["workvision_shared*"]
